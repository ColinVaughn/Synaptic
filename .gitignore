/target
/demo/
/output/
**/*.rs.bk
# Generated graph artifacts (extract / workspace build / per-member AST cache).
synaptic-out/
# Local design notes (not tracked).
/design/
# Local planning notes (not tracked), except repository-memory source artifacts.
/docs/*
!/docs/adr/
!/docs/adr/*.md
!/docs/procedures/
!/docs/procedures/*.md
# Local agent/editor integration for Synaptic (skill, hooks, MCP config). Not tracked.
/.claude/
/.mcp.json
/CLAUDE.md
/.vscode/
# Synaptic extension state and runtime evidence stay local. The API and
# vulnerability policies are versioned: an accepted security risk has to be
# reviewable in the repository, not just on the machine that accepted it.
/.synaptic/*
!/.synaptic/api-maintenance.toml
!/.synaptic/vuln-policy.toml
