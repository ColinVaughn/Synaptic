/target
/demo/
/output/
**/*.rs.bk
# Generated graph artifacts (extract / workspace build / per-member AST cache).
synaptic-out/
# Local design notes (not tracked).
/design/
# Local planning notes (not tracked).
/docs/
# Local agent/editor integration for Synaptic (skill, hooks, MCP config). Not tracked.
/.claude/
/.mcp.json
/CLAUDE.md
/.vscode/
# Synaptic extension state: workspace.json carries the control-plane project and
# workspace ids, which this public repository must never track.
/.synaptic/
